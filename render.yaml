services:
  - type: web
    name: wemadeit-studio
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn wsgi:app --timeout 120 --workers 1"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
